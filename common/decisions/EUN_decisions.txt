EUN_faction_politics = {
    icon = generic_parliament

    # --- French Faction Support ---
    EUN_support_FRA = {
        icon = generic_communism
        available = {
            has_political_power > 25
            check_variable = { var = EOB_FRA_inf value = 100 compare = less_than }
        }
        cost = 25
        visible = { always = yes }
        complete_effect = {
            add_to_variable = { var = EOB_FRA_inf value = 5 }
            if = { limit = { check_variable = { var = EOB_GER_inf value = 2 compare = greater_than_or_equals } } add_to_variable = { var = EOB_GER_inf value = -2 } }
            if = { limit = { check_variable = { var = EOB_ITA_inf value = 2 compare = greater_than_or_equals } } add_to_variable = { var = EOB_ITA_inf value = -2 } }
            if = { limit = { check_variable = { var = EOB_BEN_inf value = 1 compare = greater_than_or_equals } } add_to_variable = { var = EOB_BEN_inf value = -1 } }
        }
        ai_will_do = { factor = 1 }
        days_re_enable = 30
    }

    # --- German Faction Support ---
    EUN_support_GER = {
        icon = generic_fascism
        available = {
            has_political_power > 25
            check_variable = { var = EOB_GER_inf value = 100 compare = less_than }
        }
        cost = 25
        visible = { always = yes }
        complete_effect = {
            if = { limit = { check_variable = { var = EOB_FRA_inf value = 2 compare = greater_than_or_equals } } add_to_variable = { var = EOB_FRA_inf value = -2 } }
            add_to_variable = { var = EOB_GER_inf value = 5 }
            if = { limit = { check_variable = { var = EOB_ITA_inf value = 1 compare = greater_than_or_equals } } add_to_variable = { var = EOB_ITA_inf value = -1 } }
            if = { limit = { check_variable = { var = EOB_BEN_inf value = 2 compare = greater_than_or_equals } } add_to_variable = { var = EOB_BEN_inf value = -2 } }
        }
        ai_will_do = { factor = 1 }
        days_re_enable = 30
    }

    # --- Italian Faction Support ---
    EUN_support_ITA = {
        icon = generic_fascism
        available = {
            has_political_power > 25
            check_variable = { var = EOB_ITA_inf value = 100 compare = less_than }
        }
        cost = 25
        visible = { always = yes }
        complete_effect = {
            if = { limit = { check_variable = { var = EOB_FRA_inf value = 2 compare = greater_than_or_equals } } add_to_variable = { var = EOB_FRA_inf value = -2 } }
            if = { limit = { check_variable = { var = EOB_GER_inf value = 1 compare = greater_than_or_equals } } add_to_variable = { var = EOB_GER_inf value = -1 } }
            add_to_variable = { var = EOB_ITA_inf value = 5 }
            if = { limit = { check_variable = { var = EOB_BEN_inf value = 2 compare = greater_than_or_equals } } add_to_variable = { var = EOB_BEN_inf value = -2 } }
        }
        ai_will_do = { factor = 1 }
        days_re_enable = 30
    }

    # --- Benelux Faction Support ---
    EUN_support_BEN = {
        icon = generic_democracy
        available = {
            has_political_power > 25
            check_variable = { var = EOB_BEN_inf value = 100 compare = less_than }
        }
        cost = 25
        visible = { always = yes }
        complete_effect = {
            if = { limit = { check_variable = { var = EOB_FRA_inf value = 1 compare = greater_than_or_equals } } add_to_variable = { var = EOB_FRA_inf value = -1 } }
            if = { limit = { check_variable = { var = EOB_GER_inf value = 2 compare = greater_than_or_equals } } add_to_variable = { var = EOB_GER_inf value = -2 } }
            if = { limit = { check_variable = { var = EOB_ITA_inf value = 2 compare = greater_than_or_equals } } add_to_variable = { var = EOB_ITA_inf value = -2 } }
            add_to_variable = { var = EOB_BEN_inf value = 5 }
        }
        ai_will_do = { factor = 1 }
        days_re_enable = 30
    }
}
